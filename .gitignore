# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.cover
.hypothesis/

# Generated files (keep requirements.txt in repo but ignore lock files)
requirements*.txt.lock

# IDEs
.vscode/
.idea/

# AI
CODE_REVIEW*

# Temporary extraction scripts (session-specific, not reusable)
# Note: These patterns prevent creating temporary wrapper scripts
scripts/extract_*_*.py
scripts/run_extraction*.py
# Person-specific combine/upload scripts (session-specific, not reusable)
scripts/combine_and_upload_*_*.py
session.log

# OS-specific
.DS_Store
Thumbs.db

# Data and Config
data/
config/config.json
config/channels.json
config/people.json
slack_exports/
browser_exports/
token.json
.env
# Temporary data/output files (missing dates lists, filtered outputs, etc.)
*_missing_dates.json
*_filtered.json
*_combined*.json

# Google Drive credentials (keep example files in config/)
*credentials*.json
!config/
